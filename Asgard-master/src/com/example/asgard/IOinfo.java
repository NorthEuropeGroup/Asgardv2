package com.example.asgard;

import java.io.*;

public class IOinfo {
	public static void main(String[] args){
		/*

		Info charInfo = new Info();
		try{
			DataOutputStream dataOPS=new DataOutputStream(new FileOutputStream(Info.txt));
			dataOPS.writeUTF(charInfo.getID());
			dataOPS.writeInt(charInfo.getCards());
			dataOPS.writeInt(charInfo.getMoney());
		}
		dataOPS.flush();
		dataOPS.close();
	*/	
	}
	
}
